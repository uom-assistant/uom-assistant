// import * as version from '../../public/version.json';

/**
 * Update localStorage and IndexedDB after updated
 */
export default () => {
    // const oldVer = localStorage.getItem('uoma_version');
    // if (oldVer === false || version.version === oldVer) {
    //     return;
    // }
};
